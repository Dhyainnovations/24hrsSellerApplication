import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sellermobo.app',
  appName: 'Seller App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
